# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-Workout-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS-Workout-app

  target 'iOS-Workout-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-Workout-appUITests' do
    # Pods for testing
  end

end
